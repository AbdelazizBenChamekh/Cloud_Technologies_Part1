# Лабораторная работа(*): Анализ конфигурации веб-сервера с помощью ffuf, curl и nginx

---

## 🔹 Введение

Для данной лабораторной работы мы выбрали сайт **https://chelschool1.ru/**.

Целью лабораторной работы является **поиск ошибок конфигурации веб-сервера** и потенциальных уязвимостей с помощью инструментов `ffuf`, `curl` и анализа ответов сервера.

<img width="1920" height="979" alt="image" src="https://github.com/user-attachments/assets/143ea625-da22-4afb-a04e-ee4d1be2a514" />


---

## 🔹 Установка и подготовка инструментов

Перед началом работы были установлены:
- **Golang** — для запуска `ffuf`
- **SecLists** — словари для подбора расширений и директорий

```bash
sudo apt install golang-go
git clone https://github.com/danielmiessler/SecLists.git
```
🔹 Проверка расширений страниц (ffuf)

Для начала проверим, какие расширения поддерживаются сайтом, командой:
```bash
ffuf -w ~/SecLists/Discovery/Web-Content/web-extensions.txt -u "https://chelschool1.ru/" -ic*
```

<img width="726" height="498" alt="image" src="https://github.com/user-attachments/assets/1c561542-fcc1-4f33-9ff4-049f3a5d3dee" />


Результаты показали, что сервер обрабатывает запросы с расширениями .php, .html, .cgi и другими.
Однако код 200 не возвращается, только 301 (редирект) и 403 (доступ запрещен) — значит, настроена единая заглушка для таких запросов.

🔹 4. Проверка страницы /administrator

Проверим страницу напрямую с помощью curl:

```bash
curl -sS -I https://chelschool1.ru/administrator
curl -sS https://chelschool1.ru/administrator | head -n 10
```


Результат:

```bash
HTTP/2 200 
server: nginx-reuseport/1.21.1
content-type: text/html
content-length: 273
```


<img width="721" height="215" alt="image" src="https://github.com/user-attachments/assets/6b8167a8-b1f7-4078-a0a0-15054094a603" />

<img width="963" height="889" alt="image" src="https://github.com/user-attachments/assets/37b9281d-7318-49b0-85e0-e7c32c99bbd8" />

В теле страницы видно, что сайт использует скрипт установки cookie и перезагрузку страницы.
Это подтверждает, что интерфейс действительно существует, хоть и защищён.

🔹 5. Анализ ответов index.php и index.html
```bash
curl -I https://chelschool1.ru/index.php
curl -I https://chelschool1.ru/index.html
```


Результаты:

```bash
HTTP/2 200 
server: nginx-reuseport/1.21.1
x-powered-by: PHP/7.3.31
x-content-powered-by: K2 v2.7.0 (by JoomlaWorks)

HTTP/2 404 
server: nginx-reuseport/1.21.1
x-powered-by: PHP/7.3.31
```


<img width="684" height="468" alt="image" src="https://github.com/user-attachments/assets/1796305e-b7b9-4183-9497-ceac9a789ebb" />

**Заключение**

Взлом сайта не был успешен, однако в процессе были найдены типичные уязвимости конфигурации,
которые могут привести к раскрытию административных панелей и технологического стека.

Работа показала, как простыми командами ffuf и curl можно выявить потенциальные ошибки в настройке nginx.
